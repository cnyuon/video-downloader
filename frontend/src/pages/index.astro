---
// This is the global root entrypoint.
// Astro does not natively redirect / to /en/ when prefixDefaultLocale is false across all deployments
// So we catch the raw domain here and force the user into the default locale.
return Astro.redirect('/en/');
---
